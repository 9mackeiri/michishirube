# CLAUDE.md（michishirube）

Instagram → このLP → 公式LINE の流れで使う、1ページだけのLP。GitHub Pages で公開している。

## 公開リポジトリのルール

- このリポジトリは Public。入れてよいのは LP のページ（`index.html`・`style.css`・`README.md`・この `CLAUDE.md`）と、ページに表示する画像（`assets/`）だけ
- 台本・作業メモ・スクリプト・元の大きな画像・APIキー・`.env` 類は入れない。作業用のファイルは `work/`（gitの管理外）に置く
- 画像は LP で表示する大きさに縮小してから `assets/` に入れる（WebP、1枚あたり概ね150KB以下）
- コミット前に `git status` と `git diff --cached --stat` で、入るファイルの一覧を必ず確認する

## 文言のルール

- 書かれていない実績・数字・約束を足さない。「誰でも」「必ず」「簡単に」などの保証の言葉を使わない
- 実績の近くには「成果には個人差があります」、仮名の人物の近くには「※仮名」を入れる
- 画像に焼き込まれた文字も文言として扱う。載せない文言が写っている画像は切り抜いて外す

## 公式LINEボタン

- 行き先はすべて `https://lin.ee/wJWC9oc`。ボタンの文言は「LINEで豪華50大特典を受け取る」
- ページの途中（成果報告の後）と最後の2か所。増減したら完了報告に数を書く

## 作りのルール

- スマホ（幅390px）前提。パソコンでは中央に最大480px幅で表示し、崩れないこと
- アクセス解析・フォーム・外部スクリプト・外部フォントは入れない（システムフォントのみ）
- 見た目の系統：青の光の背景（`assets/bg-blue.webp`）、金の立体文字（`.gold`）、白いカード
- OGP の `og:url`・`og:image` は絶対URL（`https://9mackeiri.github.io/michishirube/...`）

## 確認の手順

- ページ全体のスクリーンショットをスマホ幅（390px）とパソコン幅（1280px）で撮り、崩れ・文字の切れ・横はみ出しを見る
- 公開後は公開URLを開き、すべての LINE ボタンの行き先を確認する
